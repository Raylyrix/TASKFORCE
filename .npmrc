# Ignore optional dependencies that might fail to build
optional=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Ignore scripts during install to avoid build failures
ignore-scripts=false
# Use pnpm for better dependency management
package-manager=pnpm@8.15.9
